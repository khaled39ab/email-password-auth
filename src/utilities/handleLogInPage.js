const handleEmailBlur = (event) => {
    return (event.target.value);
}

const handlePasswordBlur = (event) =>{
    return (event.target.value);
}

export {
    handleEmailBlur,
    handlePasswordBlur
};